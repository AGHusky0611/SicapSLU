import 'package:flutter/material.dart';

class SicapColors {
  // The primary blue from your SICAP organization branding
  static const Color primaryBlue = Color(0xFF1A56DB);
  
  // The vibrant yellow used for the SICAP action buttons (Sign In, Join, Pay)
  static const Color actionYellow = Color(0xFFFFD700);
  
  // Supporting colors for the modular dashboard
  static const Color background = Color(0xFFF9FAFB);
  static const Color surface = Colors.white;
  static const Color errorRed = Color(0xFFD32F2F);
}